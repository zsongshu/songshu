# 2014年面试准备材料



可靠组播；
udp协议，D类地址，降低服务器负载和带宽； 225.168.0.0


源主机（即组播源）只发送一份数据，其目的地址为组播组地址；组播组中的所有接收者都可收到同样的数据拷贝，并且只有组播组内的主机可以接收该数据，而其它主机则不能收到。能够大量节约网络带宽、降低网络负载。

对问题的分析：1、降低文件同步速度 2、 3、适当的丢包可以接受

包大小的考虑，避免在链路层传输过程中进行拆包，UDP包数据长度是1024，头有476字节；

1500字节被称为链路层的MTU(最大传输单元). netstat -i 查看

UDP头4个域，每个域2个字节
TCP、IP均20个字节

3) 远程过程调用中间件（RPC）    CORBA中间件：borland的visibroker  
4) 消息中间件（MOM）            JMS： IBM 的 MQSeries，BEA的 Weblogic，Apache activeMQ
5) 交易中间件（TPM）            BEA的tuxdo

动态内存池；

插入排序、冒泡排序、选择排序 N×N
下面的排序：N\*logN
快速排序：选择一个基数，比基数小的放左边，大的放右边

树的遍历：先序、中序、后序

图的遍历：深度优先、广度优先

paxos一致性算法 PAXOS的基本思想是只有提案者中的一个多数派批准一个提案
1、分布式存储多个节点数据的一致性
2、通过算法管理name service
3、大型系统的配置服务
4、用户访问权限，一旦权限变化，所有的地方都应该实时变化
5、票务系统
6、号码分配

Hadoop框架中最核心设计就是：HDFS和MapReduce.HDFS提供了海量数据的存储,MapReduce提供了对数据的计算.





    Created at: 2024-03-10T08:27:45+08:00
    Updated at: 2024-03-10T10:42:23+08:00

