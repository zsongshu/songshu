# 上云

3）成本
Q:系统选择了哪几种机型，为什么使用这些机型；
A:对服务器没有强制要求，普通服务器即可。

Q:计算产品的成本；
A:每台服务器价格＊3（最小集群配置需要3台服务器）


4）单元化
Q:是否做到单元化部署；
A:是。

Q:是否依赖共享区，如果依赖共享区，列出哪些组件部署在共享区；
A:仅依赖共享区Diamond。

Q:是否依赖弹内，如果依赖弹内，列出哪些组件部署在弹内；
A:不依赖。

Q:哪些组件是global的，这些组件如何部署的；
A:没有global组件。

5）依赖
Q:系统依赖哪些云产品；
A:TXC本身不依赖其他云产品，控制台依赖RDS。

Q:系统依赖哪些中间件组件；
A:依赖Diamond。

Q:系统依赖哪些集团内组件或服务；
A:依赖Diamod。

6）运维
Q:如何做到灰度发布；
A:TXCServer通过Diamond实现上下线和负载均衡。版本升级时，部分TXCServer先从Diamond将自己移除（下线），待完成升级后重新上线，一旦发生异常，可随时回滚。

Q:发布和日常运维如何做到白屏化；
A:

Q:如何对运维操作进行审计；
A:TXC采集并保存客户端及资源管理器的连接信息、采集并保存每一个全局事务及事务分支的状态，提供给TXC console供审计。

7）SLA
提供商业SLA以及检测方法；
提供设计SLA以及检测方法；
提供Open API的SLA保证以及检测方法；

8）虚拟化（云化）方案
如何实现多租户的虚拟化；
资源隔离是怎么做的；
当资源出现争抢时，系统如何检测和解决；

    Created at: 2024-03-09T22:05:50+08:00
    Updated at: 2024-03-09T22:05:50+08:00

